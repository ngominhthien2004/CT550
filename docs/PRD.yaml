public_user_search:
  summary: "Cung cấp tìm kiếm người dùng công khai trên /search với tab User và API công khai."
  routes:
    - path: "/search"
      purpose: "Bổ sung tab User để tìm kiếm người dùng công khai."
    - path: "/api/users/search"
      purpose: "API tìm kiếm người dùng công khai, chỉ trả về trường công khai."
  scope:
    in_scope:
      - "Tìm kiếm người dùng công khai theo tên người dùng hoặc tên hiển thị."
      - "Hỗ trợ phân trang với page và limit (giới hạn limit theo cấu hình)."
      - "Chỉ trả về trường công khai (không có email, role)."
    out_of_scope:
      - "Tìm kiếm người dùng theo tiêu chí riêng tư hoặc nội bộ."
      - "Loại bỏ điều hướng hoặc thay đổi logic tìm kiếm tác phẩm hiện tại."
  acceptance_criteria:
    - "API /api/users/search hoạt động với q, page, limit và trả về kết quả phân trang."
    - "Tab User trên /search hiển thị kết quả tìm kiếm người dùng công khai."
    - "Dữ liệu trả về không bao gồm trường nhạy cảm như email hoặc role."
  assumptions:
    - "Trang /search đã tồn tại và đang hoạt động ổn định."

topbar_search_unified:
  summary: "Hợp nhất ô tìm kiếm và bộ chọn phạm vi trên top bar thành một control duy nhất theo phong cách Pixiv."
  scope:
    in_scope:
      - "Nhúng bộ chọn phạm vi vào trong ô tìm kiếm ở chế độ compact."
      - "Thống nhất trạng thái hover/focus cho input và nút phạm vi như một control duy nhất."
      - "Dropdown được neo theo container chung, không lệch vị trí."
      - "Chỉ cho phép một dropdown mở tại một thời điểm (history hoặc phạm vi)."
    out_of_scope:
      - "Thay đổi hành vi tìm kiếm hoặc routing hiện có."
      - "Thêm API mới hoặc thay đổi backend."
  acceptance_criteria:
    - "Top bar hiển thị một control tìm kiếm thống nhất, không còn layout tách rời."
    - "Nhãn phạm vi hiển thị trên desktop theo mặc định."
    - "Dropdown phạm vi neo đúng vào container chung và căn thẳng hàng với input."
    - "History dropdown và phạm vi không mở đồng thời."
    - "Trạng thái focus/hover nhất quán và hỗ trợ điều hướng bằng bàn phím."
  assumptions:
    - "Không cần thay đổi backend cho UI top bar."

ai_detection_on_upload:
  summary: "Phát hiện nội dung AI khi tải tác phẩm và cảnh báo trong UI."
  routes:
    - path: "/api/ai/detect-image"
      purpose: "Dùng lại dịch vụ phát hiện AI (HuggingFace qua ai.controller)."
  scope:
    in_scope:
      - "Thực hiện phát hiện AI ở backend trong luồng upload tác phẩm."
      - "Gọi dịch vụ /api/ai/detect-image hiện có để lấy xác suất AI."
      - "Ngưỡng cảnh báo cấu hình qua env/config (mặc định 70%)."
      - "Tự động gắn tag AI cho tác phẩm khi xác suất > 70%."
      - "Hiển thị cảnh báo dưới ảnh trong giao diện upload khi vượt ngưỡng."
    out_of_scope:
      - "Thay đổi mô hình AI hoặc thay đổi nguồn dịch vụ phát hiện."
      - "Thêm workflow khiếu nại hoặc duyệt lại kết quả AI."
  acceptance_criteria:
    - "Backend upload gọi /api/ai/detect-image và nhận được xác suất AI."
    - "Ngưỡng cảnh báo lấy từ cấu hình và có thể điều chỉnh qua env/config."
    - "Khi xác suất > 70%, tác phẩm được gắn tag AI tự động."
    - "UI upload hiển thị cảnh báo dưới ảnh khi vượt ngưỡng."
  assumptions:
    - "/api/ai/detect-image đã sẵn có và ổn định."

artist_drawing_page:
  summary: "Trang vẽ cho nghệ sĩ trong frontend, tập trung vẽ cục bộ và xuất file."
  routes:
    - path: "/draw"
      purpose: "Trang vẽ mới tích hợp trong ứng dụng frontend hiện có."
  ui_style:
    reference: "Phong cách tương tự Pixiv Sketch: gọn, tập trung canvas, công cụ tối giản."
  features:
    - "Bút/brush với điều chỉnh kích thước."
    - "Tẩy (eraser)."
    - "Hoàn tác/khôi phục (undo/redo)."
    - "Layer cơ bản (thêm/xóa/ẩn/hiện, đổi thứ tự)."
    - "Nhập ảnh nền/ảnh tham chiếu."
    - "Xuất PNG/JPG cục bộ."
    - "Bảng màu + eyedropper."
    - "Zoom/pan canvas."
  tech_recommendation:
    goal: "Ưu tiên thư viện canvas nhẹ, tích hợp nhanh, hỗ trợ layer, export, và tool cơ bản."
    options:
      - "Konva.js (ổn định, hỗ trợ layer tốt, API trực quan)."
      - "Fabric.js (mạnh về object, cần đánh giá khả năng layer/tool phù hợp)."
    note: "Chưa chốt thư viện, cần quyết định trước khi triển khai."
  scope:
    in_scope:
      - "Chỉ frontend: canvas vẽ cục bộ và xuất file ngay trên trình duyệt."
      - "Tích hợp trang vẽ vào layout/router hiện có."
      - "Không phụ thuộc backend hoặc lưu trữ server."
    out_of_scope:
      - "Lưu đám mây, đồng bộ tài khoản, hay chia sẻ tác phẩm."
      - "Collab real-time hoặc chat."
      - "Tạo API mới hoặc thay đổi database."
  acceptance_criteria:
    - "Route /draw hoạt động trong frontend hiện có, điều hướng không lỗi."
    - "Canvas cho phép vẽ bằng brush, tẩy, đổi kích thước và undo/redo."
    - "Layer cơ bản hoạt động: thêm/xóa/ẩn/hiện/đổi thứ tự."
    - "Nhập ảnh nền/ảnh tham chiếu hiển thị đúng trên canvas."
    - "Xuất PNG/JPG tải về cục bộ thành công."
    - "Palette và eyedropper chọn màu chính xác."
    - "Zoom/pan mượt, không làm mất dữ liệu vẽ."
  assumptions:
    - "Không yêu cầu backend cho phiên bản đầu."
    - "Không yêu cầu lưu bản vẽ sau khi đóng trang."

needs_clarification:
  - "Thông điệp cảnh báo dưới ảnh trong giao diện upload chưa chốt nội dung."
  - "Chọn thư viện canvas chính (Konva.js vs Fabric.js) và lý do ưu tiên."
  - "Giới hạn kích thước canvas/độ phân giải xuất file (ví dụ: tối đa 4K?)"
